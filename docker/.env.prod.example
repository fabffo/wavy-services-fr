# ─────────────────────────────────────────────────────────────────
# Variables d'environnement — PRODUCTION
# Copier ce fichier en .env.prod et renseigner les valeurs
# ─────────────────────────────────────────────────────────────────

# URL publique du frontend (domaine production sans www)
FRONTEND_URL=https://wavyservices.fr

# Mot de passe PostgreSQL (générer une chaîne aléatoire sécurisée)
# Exemple : openssl rand -hex 24
POSTGRES_PASSWORD=changer_ce_mot_de_passe_fort

# Clé secrète JWT (64 caractères aléatoires minimum)
# Générer : openssl rand -hex 32
# IMPORTANT : différente de la preprod !
JWT_SECRET=changer_cette_cle_secrete_64_caracteres_minimum_tres_important

# Clé API Resend pour l'envoi d'emails (https://resend.com)
RESEND_API_KEY=re_votre_cle_resend
