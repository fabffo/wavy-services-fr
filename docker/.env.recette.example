# ─────────────────────────────────────────────────────────────────
# Variables d'environnement — RECETTE (locale)
# Copier ce fichier en .env.recette et renseigner les valeurs
# ─────────────────────────────────────────────────────────────────

# URL du frontend en recette locale
FRONTEND_URL=http://localhost:8082

# Mot de passe PostgreSQL local
POSTGRES_PASSWORD=wavy_recette_password

# Clé secrète JWT (peut être une valeur simple en recette)
JWT_SECRET=recette_jwt_secret_local_32chars_min

# Clé API Resend — laisser vide pour désactiver les emails en recette
RESEND_API_KEY=
